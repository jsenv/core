# property are different

```js
assert({
  actual: {
    a: {
      b: false,
    },
  },
  expect: {
    a: {
      b: true,
    },
  },
});
```

![img](<./assert_scratch/property are different.svg>)

