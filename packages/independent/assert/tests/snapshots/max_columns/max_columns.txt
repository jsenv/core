# double slash and truncate line
actual: "/dmail/documents/dev/jsenv-core/node_mo…"
expect: "/dmail/documents/dev/jsenv-core/node_mo…"

# at removed char
actual: …"r"
expect: …"r_1234"…

# at added char
actual: …"r_123456"…
expect: …"r"

# at removed char 2
actual: …"ring"
expect: …"ring_123456789"

# at added char 2
actual: …"ring_123456789"
expect: …"ring"

# at removed char 3
actual: …"g"
expect: …"g_123456"…

# at added char 3
actual: …"g_123456"…
expect: …"g"

# string open quote
actual: …
expect: …

# string first char
actual: …
expect: …

# string second char
actual: …
expect: …

# string third char
actual: "a"…
expect: "A"…

# string fourth char
actual: "ab"…
expect: "AB"…

# string last char
actual: "abcdefgh"…
expect: "ABCDEFGH"…

# string close quote
actual: "abcdefghij"
expect: "ABCDEFGHIJ"

# at property value
actual: {
  zzz: "abcdefghi"…,
}
expect: {
  zzz: "ABCDEFGHI"…,
}

# at property key
actual: {
  …"roperty key that will be truncated",
}
expect: {
  …"roperty key that will be truncated",
}

# at property name last char
actual: {
  …cdefgh,
}
expect: {
  …cdefgh,
}

# at property name separator
actual: {
  abcdefgh,
}
expect: {
  abcdefgh,
}

# at space after property name separator
actual: {
  abcdefgh,
}
expect: {
  abcdefgh,
}

# at property value first char
actual: {
  abcdefgh,
}
expect: {
  abcdefgh,
}

# at property value second char
actual: {
  abcdefgh: …,
}
expect: {
  abcdefgh: …,
}

# at property value second char (and value width is 1)
actual: {
  abcdefgh: 0,
}
expect: {
  abcdefgh: 1,
}

# at property value third char
actual: {
  abcdefgh: t…,
}
expect: {
  abcdefgh: f…,
}

# url search param modified, middle of long params
actual: "?this_is_rela…&foo=a"
expect: "?this_is_rela…&foo=b"

# long url diff at end
actual: "…example_that_is…/dir/file.txt"
expect: "…example_that_is…/dir/file.css"

# long url diff at start
actual: "http://example_that_is_quite_…"
expect: "file://example_that_is_quite_…"

# long url diff in the middle
actual: "…example_that_is_quite_long.c…"
expect: "…example_that_aa_quite_long.c…"

# long url diff start middle end
actual: "http://example_that_is_quite_…"
expect: "file://example_that_aa_quite_…"

