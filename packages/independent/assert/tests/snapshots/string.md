# many diff in long string, only first is shown truncated

```js
assert({
  actual: "a2cde6ghijk",
  expected: "a3cde7ghijk",
  maxColumns: 18,
});
```

![img](<./string/many diff in long string, only first is shown truncated.svg>)

