# [GET_MANY that never ran is not called after POST](../../resource_graph_parent_rerun.test.js)

```js
let getManyCallCount = 0;
const USER = resource("user", {
  GET_MANY: async () => {
    getManyCallCount++;
    return [{ id: 1, name: "Alice" }];
  },
  POST: async ({ name }) => ({ id: 2, name }),
});

// Intentionally do NOT run GET_MANY
await USER.POST({ name: "Bob" });
await waitForRerun();

return { getManyCallCount };
```

```js
{
  "getManyCallCount": 0
}
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
