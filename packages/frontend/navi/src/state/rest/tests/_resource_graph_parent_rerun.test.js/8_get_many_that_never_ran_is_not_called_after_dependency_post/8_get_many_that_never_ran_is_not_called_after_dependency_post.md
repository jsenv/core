# [GET_MANY that never ran is not called after dependency POST](../../resource_graph_parent_rerun.test.js)

```js
let getManyCallCount = 0;
const OWNER = resource("owner", {
  POST: async ({ name }) => ({ id: 1, name }),
});
resource("dependent", {
  GET_MANY: async () => {
    getManyCallCount++;
    return [];
  },
  dependencies: [OWNER],
});

// Intentionally do NOT run DEPENDENT.GET_MANY
await OWNER.POST({ name: "test" });
await waitForRerun();

return { getManyCallCount };
```

```js
{
  "getManyCallCount": 0
}
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
