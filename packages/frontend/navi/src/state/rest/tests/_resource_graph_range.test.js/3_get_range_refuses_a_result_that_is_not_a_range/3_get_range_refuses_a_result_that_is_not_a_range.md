# [GET_RANGE refuses a result that is not a range](../../resource_graph_range.test.js)

```js
const GAME = resource("game", {
  GET_RANGE: async () => [{ id: 1 }],
});

try {
  await GAME.GET_RANGE({ start: 0, limit: 10 });
  return "no error";
} catch (e) {
  return e.message;
}
```

```js
game.GET_RANGE must return { items, start, count }, received an array of 1 item.
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
