# [throws when called on a non-scoped resource](../../sync_owned_resource_to_signals.test.js)

```js
const USER = resource("user", {
  idKey: "id",
  uniqueKeys: ["username"],
});

const ownerSignal = signal("whatever");
const someSignal = signal("value");
let error = null;
try {
  syncOwnedResourceToSignals(USER, ownerSignal, {
    username: someSignal,
  });
} catch (e) {
  error = e.message;
}

return { error };
```

```js
{
  "error": 'syncOwnedResourceToSignals: "user" is not a scoped resource (scopedMany/scopedOne). Use syncResourceToSignals instead.'
}
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
