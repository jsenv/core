# [GET_MANY dataSignal updates when an item in the list is updated](../../resource_graph_many.test.js)

```js
const USER = resource("user", {
  GET_MANY: async () => [
    { id: 1, name: "Alice" },
    { id: 2, name: "Bob" },
  ],
  PATCH: async ({ id, name }) => ({ id, name }),
});

await USER.GET_MANY.run();
const dataAfterGetMany = USER.GET_MANY.dataSignal.value;

await USER.PATCH({ id: 1, name: "Alice Updated" });
const dataAfterPatch = USER.GET_MANY.dataSignal.value;

return { dataAfterGetMany, dataAfterPatch };
```

```js
{
  "dataAfterGetMany": [
    {
      "id": 1,
      "name": "Alice"
    },
    {
      "id": 2,
      "name": "Bob"
    }
  ],
  "dataAfterPatch": [
    {
      "id": 1,
      "name": "Alice Updated"
    },
    {
      "id": 2,
      "name": "Bob"
    }
  ]
}
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
