# [observeItemProperties unsubscribe stops receiving callbacks](../../array_signal_store_observe.test.js)

```js
const store = arraySignalStore([], "id", { uniqueKeys: ["username"] });
store.upsert({ id: 1, username: "alice", role: "user" });

const usernameSignal = store.signalForKey("username", {
  peek: () => "alice",
  value: "alice",
});

const calls = [];
const unsubscribe = store.observeItemProperties(
  usernameSignal,
  (mutations) => {
    calls.push(mutations);
  },
);

store.upsert({ id: 1, role: "admin" });
unsubscribe();
store.upsert({ id: 1, role: "superadmin" });

return { calls };
```

```js
{
  "calls": [
    {
      "role": {
        "oldValue": "user",
        "newValue": "admin",
        "target": {
          "id": 1,
          "username": "alice",
          "role": "user"
        },
        "newTarget": {
          "id": 1,
          "username": "alice",
          "role": "admin"
        }
      }
    }
  ]
}
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
