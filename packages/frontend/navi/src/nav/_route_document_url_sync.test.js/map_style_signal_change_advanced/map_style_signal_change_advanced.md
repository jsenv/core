# [map style signal change advanced](../../route_document_url_sync.test.js)

```js
const navToCalls = [];
setRouteIntegration({
  navTo: (url) => {
    navToCalls.push(url);
    // Simulate real browser integration: update routes to reflect new URL
    updateRoutes(url);
  },
});
const mapStyleSignal = stateSignal("street");
const MAP_ROUTE = route("/map/", {
  searchParams: { style: mapStyleSignal },
});
const MAP_ISOCHRONE_ROUTE = route(`/map/isochrone`);
const { updateRoutes, clearRoutes } = setupRoutes([
  MAP_ROUTE,
  MAP_ISOCHRONE_ROUTE,
]);
try {
  updateRoutes(`${baseUrl}/map/isochrone`);
  mapStyleSignal.value = "satellite";
  const afterUpdateToSattelite = {
    current_url: MAP_ISOCHRONE_ROUTE.url,
    map_style_signal: mapStyleSignal.value,
    navToCalls: [...navToCalls],
  };
  navToCalls.length = 0;
  mapStyleSignal.value = "street";
  const afterRestoreToStreet = {
    current_url: MAP_ISOCHRONE_ROUTE.url,
    map_style_signal: mapStyleSignal.value,
    navToCalls: [...navToCalls],
  };
  return {
    after_update_to_sattelite: afterUpdateToSattelite,
    after_restore_to_street: afterRestoreToStreet,
  };
} finally {
  clearRoutes();
  globalSignalRegistry.clear();
  setRouteIntegration(null);
}
```

```js
{
  "after_update_to_sattelite": {
    "current_url": "http://127.0.0.1/map/isochrone?style=satellite",
    "map_style_signal": "satellite",
    "navToCalls": [
      "http://127.0.0.1/map/isochrone?style=satellite"
    ]
  },
  "after_restore_to_street": {
    "current_url": "http://127.0.0.1/map/isochrone",
    "map_style_signal": "street",
    "navToCalls": [
      "http://127.0.0.1/map/isochrone"
    ]
  }
}
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
