# [route.url should update when signal changes](../../route.test.js)

```js
try {
  const zoneSignal = stateSignal("paris");

  const { MAP_ROUTE } = setupRoutes({
    MAP_ROUTE: `/map?zone=${zoneSignal}`,
  });

  const urlBefore = MAP_ROUTE.url;
  
  zoneSignal.value = "london";
  
  const urlAfter = MAP_ROUTE.url;

  return {
    url_before: urlBefore,
    url_after: urlAfter,
    should_be_different: urlBefore !== urlAfter,
  };
} finally {
  clearAllRoutes();
  globalSignalRegistry.clear();
}
```

```js
{
  "url_before": "http://127.0.0.1/map",
  "url_after": "http://127.0.0.1/map?zone=london",
  "should_be_different": true
}
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
