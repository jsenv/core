# [outputSignal is updated by child action created via bindParams](../../actions.test.js)

```js
const userSignal = signal(null);
const action = createAction(
  async ({ id }) => {
    return { id, name: "Alice" };
  },
  { outputSignal: userSignal },
);

const childAction = action.bindParams({ id: 42 });
const before = userSignal.value;
await childAction.run();
const after = userSignal.value;

return { before, after };
```

```js
{
  "before": null,
  "after": {
    "id": 42,
    "name": "Alice"
  }
}
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
