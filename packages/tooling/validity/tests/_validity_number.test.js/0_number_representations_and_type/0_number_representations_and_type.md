# [number representations and type](../../validity_number.test.js)

```js
const [validityString, applyOnString] = createValidity({
  type: "number",
  customRepresentation: "string",
});

const cases = [
  42,
  3.14,
  "123",
  "3.14",
  "not a number",
  Infinity,
  undefined,
];

const reprCell = (v) => {
  return cell(
    v.value !== undefined ? humanize(v.value) : "[[CANNOT_CONVERT]]",
  );
};

const grid = [
  [
    cell("value"),
    cell("representation: string"),
    cell("valid"),
    cell("invalid message"),
  ],
  ...cases.map((value) => {
    applyOnString(value);
    return [
      cell(humanize(value)),
      reprCell(validityString),
      cell(validityString.valid ? "✓" : "✗"),
      cell(validityString.type ?? "-"),
    ];
  }),
];

return renderTable(grid, { borderCollapse: true });
```

```js
┌────────────────┬────────────────────────┬───────┬──────────────────┐
│ value          │ representation: string │ valid │ invalid message  │
├────────────────┼────────────────────────┼───────┼──────────────────┤
│ 42             │ [[CANNOT_CONVERT]]     │ ✓     │ -                │
├────────────────┼────────────────────────┼───────┼──────────────────┤
│  3.14          │ [[CANNOT_CONVERT]]     │ ✓     │ -                │
├────────────────┼────────────────────────┼───────┼──────────────────┤
│ "123"          │ [[CANNOT_CONVERT]]     │ ✗     │ must be a number │
├────────────────┼────────────────────────┼───────┼──────────────────┤
│ "3.14"         │ [[CANNOT_CONVERT]]     │ ✗     │ must be a number │
├────────────────┼────────────────────────┼───────┼──────────────────┤
│ "not a number" │ [[CANNOT_CONVERT]]     │ ✗     │ must be a number │
├────────────────┼────────────────────────┼───────┼──────────────────┤
│ Infinity       │ [[CANNOT_CONVERT]]     │ ✗     │ must be finite   │
├────────────────┼────────────────────────┼───────┼──────────────────┤
│ undefined      │ [[CANNOT_CONVERT]]     │ ✗     │ must be a number │
└────────────────┴────────────────────────┴───────┴──────────────────┘
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
