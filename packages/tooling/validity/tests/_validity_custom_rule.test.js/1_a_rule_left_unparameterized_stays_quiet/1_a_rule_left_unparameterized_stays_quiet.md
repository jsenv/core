# [a rule left unparameterized stays quiet](../../validity_custom_rule.test.js)

```js
const [validity, applyOn] = createValidity({
  type: "string",
  rules: [MAX_WORDS_RULE],
});
applyOn("un deux trois quatre cinq");
return renderTable(
  [
    [cell(".valid"), cell(".maxWords")],
    [cell(humanize(validity.valid)), cell(humanize(validity.maxWords))],
  ],
  { borderCollapse: true },
);
```

```js
┌────────┬───────────┐
│ .valid │ .maxWords │
├────────┼───────────┤
│ true   │ undefined │
└────────┴───────────┘
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
