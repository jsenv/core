# [a malformed rule is refused](../../validity_custom_rule.test.js)

```js
const attempts = [
  { rules: [{ applyOn: () => null }] },
  { rules: [{ name: "noApplyOn" }] },
];
const rows = attempts.map((ruleConfig) => {
  let thrown;
  try {
    createValidity({ type: "string", ...ruleConfig });
  } catch (e) {
    thrown = e.message;
  }
  return [cell(humanize(ruleConfig.rules[0])), cell(humanize(thrown))];
});
return renderTable([[cell("rule"), cell("throws")], ...rows], {
  borderCollapse: true,
});
```

```js
┌───────────────────────────────────┬────────────────────────────────────────────────────┐
│ rule                              │ throws                                             │
├───────────────────────────────────┼────────────────────────────────────────────────────┤
│ {                                 │ "[createValidity] a rule must have a name"         │
│   "applyOn": () => {/* hidden */} │                                                    │
│ }                                 │                                                    │
├───────────────────────────────────┼────────────────────────────────────────────────────┤
│ {                                 │ '[createValidity] rule "noApplyOn" must have appl… │
│   "name": "noApplyOn"             │                                                    │
│ }                                 │                                                    │
└───────────────────────────────────┴────────────────────────────────────────────────────┘
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
