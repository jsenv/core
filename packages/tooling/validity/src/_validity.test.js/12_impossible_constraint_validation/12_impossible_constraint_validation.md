# [impossible constraint validation](../../validity.test.js)

```js
// Test case where no valid suggestion is possible
const [validity, applyOn] = createValidity({
  type: "number",
  oneOf: [10, 20, 30], // Only these values allowed
  min: 50, // But min is 50, making oneOf values impossible
});

const run = (value) => {
  applyOn(value);
  return structuredClone(validity);
};

return {
  '"15"': run("15"),
};
```

```js
{
  '"15"': {
    "type": "must be a number",
    "min": undefined,
    "oneOf": "must be one of: 10, 20, 30",
    "valid": false,
    "validSuggestion": {
      "value": 10
    }
  }
}
```

---

<sub>
  Generated by <a href="https://github.com/jsenv/core/tree/main/packages/tooling/snapshot">@jsenv/snapshot</a>
</sub>
