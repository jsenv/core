/* globals self */

// ceci, il faudrait trouver comment mais ce serais mis a jour
// si urlsToCache change, pas sur qu'un hash du tableau suffise
// mais partons du principe que si
self.cacheName = `pwa-cache-1`

// ce fichier pourrait etre généré par jsenv lors du build
self.urlsToCache = ["pwa.style.css"]

self.logsEnabled = true
