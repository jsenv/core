export const parseAndTransformJsClassicUrls = async () => {
  //   const isWorker =
  //     urlInfo.subtype === "worker" || urlInfo.subtype === "service_worker"
  // when isWorker we must parse self.importScripts
  // see babel_plugin_inline_worker_imports.js
}
