import { JSENV_PATHNAME } from "../JSENV_PATH.js"
import { NODE_PLATFORM_RELATIVE_PATH } from "./serve-node-platform.js"

export const NODE_PLATFORM_PATHNAME = `${JSENV_PATHNAME}${NODE_PLATFORM_RELATIVE_PATH}`
