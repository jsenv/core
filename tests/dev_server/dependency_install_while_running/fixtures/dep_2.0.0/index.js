// one entry per evaluation of this module: a page holding two copies of the
// package lists two versions here
export const version = "2.0.0";
window.depInstances = window.depInstances || [];
window.depInstances.push(version);
